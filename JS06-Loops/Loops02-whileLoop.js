function whileLoop() {
    let i = 5;
    // while loop :: is used when you don't know the number of iteration
    // It continues to execute as long as the specified condition is true.
    while (i > 0) {
        console.log("Count is : " ,i);
        i--;
    }
}
whileLoop();