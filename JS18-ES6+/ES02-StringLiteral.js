/*
2. We use string as Static/constant varibale,Now we use String literal for multiline and dynamic string
    Syntax = `Hello ${name}`
    -> it is also call string interpolation
*/
let name = 'Azmat alli khan';
let greet = `Hello ${name},How Are you?`;
console.log(greet);
